name: filehive
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: ^3.11.4


dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.0.0
  go_router: ^13.0.0
  dio: ^5.0.0
  shelf: ^1.4.0
  shelf_router: ^1.1.0       # ← ADD
  shelf_multipart: ^2.0.0    # ← ADD
  mime: ^1.0.4               # ← ADD
  multicast_dns: ^0.3.2
  network_info_plus: ^6.0.1
  connectivity_plus: ^6.0.0
  file_picker: ^8.0.0
  path_provider: ^2.1.0
  permission_handler: ^11.0.0


  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter


  flutter_lints: ^6.0.0


flutter:

  uses-material-design: true


